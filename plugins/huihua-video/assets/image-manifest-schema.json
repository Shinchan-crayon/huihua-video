{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["images"],
  "properties": {
    "images": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "scene_id", "file", "role", "aspect_ratio", "fit", "crop_allowed", "prompt"],
        "properties": {
          "id": {"type": "string"},
          "scene_id": {"type": "string"},
          "file": {"type": "string", "minLength": 1},
          "role": {"enum": ["color", "line", "foreground", "midground", "background", "detail"]},
          "aspect_ratio": {"type": "string", "pattern": "^[1-9][0-9]*:[1-9][0-9]*$"},
          "fit": {"const": "contain"},
          "crop_allowed": {"const": false},
          "prompt": {"type": "string", "minLength": 1}
        }
      }
    }
  }
}
