interface:
  display_name: "绘画视频插画导演"
  short_description: "调用 Image Prompt Generator 规划审核并生成完整叙事插画"
  default_prompt: "使用 $huihua-image-director 和 $image-prompt-generator 为这些场景生成完整插画方案。"
policy:
  allow_implicit_invocation: false
